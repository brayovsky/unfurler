{
  "name": "unfurler",
  "version": "0.0.1",
  "scripts": {
    "migrate": "node -r dotenv/config node_modules/migrate/bin/migrate"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongodb": "^3.5.9"
  },
  "devDependencies": {
    "migrate": "^1.7.0"
  }
}
